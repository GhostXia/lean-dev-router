interface:
  display_name: "Lean Dev Router"
  short_description: "One Sol coordinates parallel Luna and Terra workers"
  default_prompt: "Use $lean-dev-router with one sol_planner coordinating as many parallel luna_worker and terra_auditor instances as task complexity requires."
