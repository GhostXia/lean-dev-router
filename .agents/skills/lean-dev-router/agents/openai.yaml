interface:
  display_name: "Lean Dev Router"
  short_description: "Route agents by token cost and read-only latency"
  default_prompt: "Use $lean-dev-router with minimal calls and parent-led parallel read-only fan-out when it materially reduces latency."
